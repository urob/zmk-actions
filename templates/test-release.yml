name: Run tests (release)

on:
  workflow_dispatch:
  push:
    paths:
      - "dts/**"
      - "include/**"
      - "src/**"
      - "tests/**"
  pull_request:
    paths:
      - "dts/**"
      - "include/**"
      - "src/**"
      - "tests/**"

permissions:
  contents: read # check out the repository

jobs:
  test:
    uses: urob/zmk-actions/.github/workflows/run-tests.yml@v12.2.0
    with:
      toolchain: gnuarmemb
